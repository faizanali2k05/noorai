# Copy to .env and fill in
GEMINI_API_KEY=
# Gemini model used for realtime intent parsing (override if needed)
GEMINI_MODEL=gemini-2.0-flash
# Set to 1 to bypass Gemini entirely and use the regex fallback (useful for offline demo)
NOORAI_OFFLINE_MODE=0
